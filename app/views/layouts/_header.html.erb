<div class="header">
  <nav class="navbar navbar-default" role="navigation">
  <ul class="nav navbar-nav navbar-right">
    <!-- <li><a href="#">Link</a></li> -->
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">Welcome <%= current_user.name %> <span class="caret"></span></a>
      <ul class="dropdown-menu" role="menu">
        <li>
          <% if current_user %>
            <%= link_to "Login", login_url %>
          <% else %>
            <%= link_to "Logout", logout_url %>
          <% end %>
        </li>
        <li>
          <%= link_to "Account Settings", edit_user_url %>
        </li>
      </ul>
    </li>
  </ul>
</div>
